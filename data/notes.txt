- SD kaart of eMMC?
- U-Boot usb_ether of cpw/eth?

U-Boot# setenv ethact usb_ether

Kernel + DTS
- apt-get install device-tree-compiler lzma lzop

uEnv.txt:
kloadaddr=0x80800000
devtree=am335x-boneblack.dtb
dtboot=run mmcargs; fatload mmc ${mmcdev}:1 ${kloadaddr} ${bootfile} ; fatload mmc ${mmcdev}:1 ${fdtaddr} ${devtree} ; bootm ${kloadaddr} - ${fdtaddr}
uenvcmd=run dtboot
optargs=consoleblank=0

---
flash eMMC:
- Image to flash eMMC: http://beagleboard.org/latest-images
- dd image to a SD card and boot it
- wait until all 4 leds stay lit up, this will take about 45 minutes


